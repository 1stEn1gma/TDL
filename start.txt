#!/bin/bash

# Запрашиваем значение параметра param1
read -p "Введите значение параметра param1: " param1

# Запускаем команду с переданным значением параметра
uvicorn src.main:app --reload --host "$param1"